a simple note-taking app where users can:
Create notes (title & content).
List all notes.
Analyze the sentiment of a note’s content using an ML model (positive, neutral, or negative).
Store the notes locally using a database (SQLite or MongoDB)
